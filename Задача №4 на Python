# Задайте натуральное число N. Напишите программу, которая составит список простых множителей числа N.
def a(N):
    b=list()
    divisor=2
    while(divisor <=N):
        if (N % divisor)==0:
            b.append(divisor)
            N=N/divisor
    else:
        divisor+=1
        return b
